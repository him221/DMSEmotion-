# BERT fine-tuned weights

This directory is reserved for optional BERT fine-tuned `.pth` weights.

The main GUI does not require this directory. Do not commit large `.pth` files to the normal Git repository.
